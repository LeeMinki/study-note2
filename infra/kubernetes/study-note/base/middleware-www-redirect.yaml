apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: www-redirect
  namespace: study-note
spec:
  # www.DOMAIN → DOMAIN 로 영구 리디렉션
  redirectRegex:
    regex: "^https://www\\.(.*)"
    replacement: "https://${1}"
    permanent: true
