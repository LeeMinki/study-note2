apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: traefik
  namespace: kube-system
spec:
  valuesContent: |-
    ports:
      web:
        # port 80 전체 트래픽을 443으로 영구 리디렉션 (HTTP→HTTPS 전역 적용)
        redirectTo:
          port: websecure
          permanent: true
